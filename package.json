{
  "name": "hubspot-portal-sync-assessment",
  "version": "1.0.0",
  "description": "Idempotent HubSpot portal-to-portal custom object sync service",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "main": "src/handlers/serverlessHandler.js",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "example": "node scripts/example.js",
    "lint": "eslint src tests scripts"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "nodemon": "^3.1.0"
  }
}